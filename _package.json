{
  "name": "<%= _.slugify(appname) %>",
  "version": "<%= version %>",
  "private": true,
  "main": "server/index.js",
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=0.10.0"
  }
}